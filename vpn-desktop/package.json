{
  "name": "clop-vpn",
  "version": "1.0.0-beta.1",
  "productName": "Clop VPN",
  "description": "Clop VPN for Windows and Linux",
  "author": "Clop",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs",
    "smoke": "electron . --smoke-test",
    "dist": "node scripts/icon.cjs && electron-builder --win nsis --x64",
    "dist:linux": "node scripts/icon.cjs && electron-builder --linux tar.xz --x64"
  },
  "devDependencies": {
    "electron": "44.2.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "npmRebuild": false,
    "appId": "com.clop.vpn.desktop",
    "productName": "Clop VPN",
    "artifactName": "Clop-VPN-Setup-${version}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "src/**/*",
      "build/icon.png",
      "package.json",
      "TERMS.txt"
    ],
    "asar": true,
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/icon.ico",
      "requestedExecutionLevel": "asInvoker"
    },
    "linux": {
      "target": [
        "tar.xz"
      ],
      "category": "Network",
      "icon": "build/icon.png",
      "executableName": "clop-vpn",
      "artifactName": "Clop-VPN-${version}-linux-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Clop VPN",
      "runAfterFinish": false,
      "license": "TERMS.txt",
      "deleteAppDataOnUninstall": false
    }
  }
}
